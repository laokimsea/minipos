spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_minipos
    username: postgres
    password: 1234567890
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect